@model booking_facilities.Models.Sport
@{
    ViewData["Title"] = "Delete Sport";
}
<div class="row">
    <div class="col-md-4">
        <div class="card">
            <div class="card-header bg-danger text-white">
                <strong class="card-title">Delete Venue</strong>
            </div>
            <div class="card-body">

                <h3>Are you sure you want to delete this?</h3>
                <div>

                    <hr />
                    <dl class="dl-horizontal">
                        <dt>
                            @Html.DisplayNameFor(model => model.SportName)
                        </dt>
                        <dd>
                            @Html.DisplayFor(model => model.SportName)
                        </dd>
                    </dl>

                    <form asp-action="Delete">
                        <input type="hidden" asp-for="SportId" />
                        <input type="submit" value="Delete" class="btn btn-danger" />
                        <a asp-action="Index" class="btn btn-info">Back to List</a>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

